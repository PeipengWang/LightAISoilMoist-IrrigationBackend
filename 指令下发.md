Activity_main.xml 界面布局文件
Colors.xml 颜色管理
AndroidMainifest.xml  修改APP名字,在string.xml文件里修改

MainActivity.jave 主程序文件

一、修改阿里云MQTT
// 修改部分
private String productKey = "a1ODkNy3j55";   // 产品密钥
private String deviceName = "APP_Test";      // 产品名称
private String deviceSecret = "76742e2dce03afeb3046b8d0454cbf56";  // 产品Secret

// 发布
private final String pub_topic = "/a1ODkNy3j55/APP_Test/user/pub";
// 订阅
private final String sub_topic = "/a1ODkNy3j55/APP_Test/user/sub";

二、控件初始化定义
// 标签定义
// 标签定义, 显示值
TextView label1_val = findViewById(R.id.label1_val);
TextView label2_val = findViewById(R.id.label2_val);
TextView label3_val = findViewById(R.id.label3_val);
TextView label4_val = findViewById(R.id.label4_val);

// 标签定义，状态
TextView label_sta1_val = findViewById(R.id.label_sta1_val);
TextView label_sta2_val = findViewById(R.id.label_sta2_val);
TextView label_sta3_val = findViewById(R.id.label_sta3_val);

// 按键定义，开关
Button btn_sta1 = findViewById(R.id.btn_sta1);
Button btn_sta2 = findViewById(R.id.btn_sta2);
Button btn_sta3 = findViewById(R.id.btn_sta3);

// 标签定义，阈值
TextView label_set1_val = findViewById(R.id.label_set1_val);
TextView label_set2_val = findViewById(R.id.label_set2_val);
TextView label_set3_val = findViewById(R.id.label_set3_val);

// 标签定义，阈值输入
EditText edit_set1_val = findViewById(R.id.edit_set1_val);
EditText edit_set2_val = findViewById(R.id.edit_set2_val);
EditText edit_set3_val = findViewById(R.id.edit_set3_val);

// 按键定义，设置阈值
Button btn_set1 = findViewById(R.id.btn_set1);
Button btn_set2 = findViewById(R.id.btn_set2);
Button btn_set3 = findViewById(R.id.btn_set3);

三、按钮操作
1、开关操作
btn_sta1.setOnClickListener(new View.OnClickListener() {
@Override
public void onClick(View v) {
//{"method":"thing.service.property.set","id":"1202748802","params":{"Relay1":2},"version":"1.0.0"}
publish_message("{\"params\":{\"sw\":0},\"version\":\"1.0.0\"}");
}
});

2、阈值设置
btn_set1.setOnClickListener(new View.OnClickListener() {
@Override
public void onClick(View v) {
String str = edit_set1_val.getText().toString();
publish_message("{\"params\":{\"val1\":" + str + "},\"version\":\"1.0.0\"}");
}
});

四、JSON解析
String message = msg.obj.toString();
Log.d("nicecode", "handleMessage: "+ message);
try {
JSONObject jsonObjectALL = null;
jsonObjectALL = new JSONObject(message);


    int label1 = jsonObjectALL.getInt("A");
    label1_val.setText(label1 + "");
    
    int label2 = jsonObjectALL.getInt("B");
    label2_val.setText(label2 + "");
    
    int label3 = jsonObjectALL.getInt("C");
    label3_val.setText(label3 + "");
    
    int label4 = jsonObjectALL.getInt("D");
    label4_val.setText(label4 + "");
    
    int sta1 = jsonObjectALL.getInt("E");
    if(sta1 == 1)
    {
        label_sta1_val.setTextColor(Color.RED);
        label_sta1_val.setText("开启");
    }
    else
    {
        label_sta1_val.setTextColor(Color.BLACK);
        label_sta1_val.setText("正常");
    }
    
    int sta2 = jsonObjectALL.getInt("F");
    if(sta2 == 1)
    {
        label_sta2_val.setTextColor(Color.RED);
        label_sta2_val.setText("开启");
    }
    else
    {
        label_sta2_val.setTextColor(Color.BLACK);
        label_sta2_val.setText("正常");
    }
    
    int sta3 = jsonObjectALL.getInt("G");
    if(sta3 == 1)
    {
        label_sta3_val.setTextColor(Color.RED);
        label_sta3_val.setText("开启");
    }
    else
    {
        label_sta3_val.setTextColor(Color.BLACK);
        label_sta3_val.setText("正常");
    }
    
    int label_set1 = jsonObjectALL.getInt("H");
    label_set1_val.setText(label_set1 + "");
    
    int label_set2 = jsonObjectALL.getInt("I");
    label_set2_val.setText(label_set2 + "");
    
    int label_set3 = jsonObjectALL.getInt("J");
    label_set3_val.setText(label_set3 + "");

} catch (JSONException e) {
e.printStackTrace();
break;
}