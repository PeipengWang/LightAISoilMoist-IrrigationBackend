这是一个后端项目，实现从OneNet接收数据并进行实时展示，同时支持事后查询和指令下发

注意编写代码完毕后实时更新到TECH_STACK.md文档中

编写的代码要对应编写测试用例，直到测试用例通过才终止

如果需求不合理则直接显示编写一个不合理文档，不需要编写代码

jdk在 D:/jdk/jdk17目录下，使用jdk17

每次新建一个端口要记录输入输出和实例到TECH_STACK.md文档中

注意：
通过OneNet与设备相连时，不能出现两台设备的ClientID相同的情况，出现时OneNet会踢掉前面的，我的设备端已经作为设备终端建立起了连接

每次大修代码时注意用git切换分支进行修改，等我确定没问题后在合并到主分支，并删除新建的分支

每次一些修改细节加入到doc目录下的md文档，如果不存在则生成一个md方案文档，如果存在则合并