# 土壤阈值数据库设计方案

> 基于 H2DB + Spring Data JPA 的智能阈值管理系统，为苹果种植决策引擎提供动态阈值支持

---

## 一、为什么阈值不能是静态的

当前项目中，阈值被硬编码为设备属性（I/J/K/L），通过 OneNet 下发。这种方式在实际农业场景中存在根本性问题：

| 问题 | 说明 |
|------|------|
| **物候期差异** | 苹果休眠期适宜湿度 50-60%，膨大期需要 65-80%，同一阈值管全年必然出错 |
| **土壤类型差异** | 沙土田间持水量约 10-15%，黏土可达 35-40%，同一湿度数值对沙土已是干旱、对黏土仍在涝害 |
| **品种差异** | 嘎啦需冷量约 600h，红富士约 1200h，萌芽时机判断阈值完全不同 |
| **天气联动** | 未来3天有大雨时，灌溉触发阈值应临时下调，避免灌后遇雨导致涝害 |
| **历史校正** | 阈值设定后需要根据实际效果（如果树长势、产量、病害发生率）动态修正 |
| **多作物扩展** | 当前只种苹果，未来可能增加其他作物，每种作物需要独立的阈值体系 |

**结论：阈值必须是一个可按时间/条件/阶段动态变化的配置体系，而非一组固定数字。**

---

## 二、阈值体系设计

### 2.1 阈值分类

```
阈值体系
├── 基础阈值（静态基准）
│   ├── 土壤湿度下限（不同物候期 × 不同土壤类型）
│   ├── 土壤湿度上限（不同物候期 × 不同土壤类型）
│   ├── 土壤 pH 下限 / 上限（不同作物品种）
│   ├── 环境温度下限 / 上限（不同物候期）
│   └── 光照下限（不同物候期）
│
├── 动态修正（实时调整）
│   ├── 天气预报修正（降雨预报 → 临时降低灌溉阈值）
│   ├── 季节偏移（连续高温 → 逐步提高灌溉阈值）
│   └── 传感器偏差补偿（传感器漂移校准值）
│
├── 告警阈值（事件触发）
│   ├── 数据中断告警（传感器 N 分钟无数据）
│   ├── 极端值告警（pH < 4.5 或 > 8.5，温度 > 40°C）
│   ├── 趋势告警（湿度连续下降 > N%/h 持续 M 小时）
│   └── 设备故障告警（水泵开启但湿度不升）
│
└── 决策阈值（LLM 触发条件）
    ├── 灌溉决策触发阈值（偏离适宜范围多少时触发 LLM 分析）
    ├── 施肥决策触发周期（按物候期切换触发）
    ├── 病害风险阈值（温湿度组合满足特定条件时触发）
    └── 霜冻预警阈值（花期 + 预报低温 < 临界值）
```

### 2.2 核心设计理念：基准值 + 修正链

```
最终阈值 = 基准阈值 × 物候期修正系数 × 土壤类型修正系数 × 天气临时修正系数
```

例如苹果膨大期灌溉下限：
```
50%（沙土基准） × 1.3（膨大期修正） × 0.7（沙土保水差修正） × 0.85（未来3天有中雨）
= 最终触发阈值 ≈ 38.7%
```

当实时土壤湿度低于 38.7% 时，触发灌溉建议。

---

## 三、数据库表设计

### 3.1 ER 关系

```
┌─────────────────┐       ┌──────────────────────┐
│   crop_config    │       │  phenology_stage     │
│   (作物配置)      │──1:N──│  (物候期定义)         │
└────────┬────────┘       └──────────┬───────────┘
         │                           │
         │ 1:N                       │ 1:N
         ▼                           ▼
┌─────────────────┐       ┌──────────────────────┐
│ threshold_rule  │       │ stage_threshold      │
│ (阈值规则)       │◄──N:1─│ (物候期阈值)          │
└────────┬────────┘       └──────────────────────┘
         │
         │ 1:N
         ▼
┌─────────────────────┐
│ threshold_snapshot  │
│ (阈值快照/历史)      │
└─────────────────────┘

┌──────────────────┐      ┌──────────────────────┐
│ soil_config      │      │ threshold_adjustment │
│ (土壤类型配置)    │      │ (动态修正记录)        │
└──────────────────┘      └──────────────────────┘

┌──────────────────┐
│ alert_rule       │
│ (告警规则)        │
└──────────────────┘
```

### 3.2 表结构详细定义

#### 表1: `crop_config` — 作物品种配置

| 列名 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT PK | 主键 |
| crop_name | VARCHAR(64) NOT NULL | 作物名称，如"苹果" |
| variety | VARCHAR(64) NOT NULL | 品种，如"红富士"、"嘎啦" |
| tree_age | INT | 树龄（年） |
| planting_density | VARCHAR(32) | 种植密度，如"3×4m" |
| irrigation_method | VARCHAR(32) | 灌溉方式：DRIP/SPRAY/FLOOD |
| is_active | BOOLEAN DEFAULT TRUE | 是否当前激活的配置 |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

UNIQUE KEY: `(crop_name, variety, is_active)` — 同一品种只能有一个激活配置

#### 表2: `soil_config` — 土壤类型配置

| 列名 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT PK | 主键 |
| soil_type | VARCHAR(32) NOT NULL UNIQUE | 土壤类型：SAND/LOAM/CLAY/SANDY_LOAM/CLAY_LOAM |
| field_capacity | DECIMAL(5,2) | 田间持水量（%），即土壤能保持的最大含水量 |
| wilting_point | DECIMAL(5,2) | 萎蔫系数（%），植物无法吸水的临界值 |
| available_water | DECIMAL(5,2) | 有效水量 = field_capacity - wilting_point |
| infiltration_rate | DECIMAL(6,2) | 入渗速率（mm/h） |
| moisture_correction_factor | DECIMAL(4,3) DEFAULT 1.000 | 湿度修正系数，用于将标准阈值转换为该土壤的阈值 |

#### 表3: `phenology_stage` — 物候期定义

| 列名 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT PK | 主键 |
| crop_config_id | BIGINT FK → crop_config.id | 关联作物 |
| stage_order | INT NOT NULL | 阶段序号（1-7 对应休眠→落叶） |
| stage_name | VARCHAR(32) NOT NULL | 休眠期/萌芽期/花期/幼果期/膨大期/成熟期/落叶期 |
| typical_start_month | INT | 典型开始月份（1-12） |
| typical_end_month | INT | 典型结束月份（1-12） |
| gdd_threshold_low | DECIMAL(8,1) | 积温阈值下限（°C·d，>5°C 有效积温） |
| gdd_threshold_high | DECIMAL(8,1) | 积温阈值上限 |
| description | VARCHAR(255) | 阶段描述 |

#### 表4: `stage_threshold` — 物候期阈值（核心表）

| 列名 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT PK | 主键 |
| phenology_stage_id | BIGINT FK → phenology_stage.id | 关联物候期 |
| soil_config_id | BIGINT FK → soil_config.id | 关联土壤类型（NULL = 适用所有土壤） |
| property_identifier | VARCHAR(16) NOT NULL | 属性标识符：A(pH), B(土壤湿度), C(环境温度), D(环境湿度), E(光照) |
| threshold_type | VARCHAR(16) NOT NULL | 阈值类型：LOWER（下限）/ UPPER（上限）/ TARGET（目标值） |
| threshold_value | DECIMAL(10,3) NOT NULL | 阈值数值 |
| unit | VARCHAR(16) | 单位：%、°C、lux、(pH 无单位) |
| priority | INT DEFAULT 0 | 优先级，数字越大越优先 |
| is_active | BOOLEAN DEFAULT TRUE | 是否启用 |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

UNIQUE KEY: `(phenology_stage_id, soil_config_id, property_identifier, threshold_type)`

#### 表5: `threshold_rule` — 阈值规则元数据

| 列名 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT PK | 主键 |
| rule_name | VARCHAR(64) NOT NULL | 规则名称 |
| rule_type | VARCHAR(32) NOT NULL | IRRIGATION/FERTILIZATION/PH_ADJUST/FROST_ALERT/DISEASE_ALERT/HARVEST |
| property_identifier | VARCHAR(16) | 关联的属性标识符 |
| crop_config_id | BIGINT FK → crop_config.id | 关联作物 |
| trigger_expression | VARCHAR(512) | 触发表达式，如 `value < threshold_lower * 0.9` |
| action_type | VARCHAR(32) | ALERT / DECISION / AUTO_CONTROL |
| action_params | VARCHAR(1024) | JSON 格式的动作参数 |
| cooldown_minutes | INT DEFAULT 60 | 冷却时间（分钟），避免频繁重复触发 |
| is_active | BOOLEAN DEFAULT TRUE | 是否启用 |

#### 表6: `threshold_snapshot` — 阈值变更历史

| 列名 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT PK | 主键 |
| stage_threshold_id | BIGINT FK | 被修改的阈值 ID |
| old_value | DECIMAL(10,3) | 修改前的值 |
| new_value | DECIMAL(10,3) | 修改后的值 |
| change_reason | VARCHAR(512) | 变更原因（人工调整 / 自动修正 / 历史数据校正） |
| change_source | VARCHAR(32) | MANUAL / AUTO_CALIBRATION / LLM_DECISION |
| operator | VARCHAR(64) | 操作人（人工）或触发模块（自动） |
| changed_at | TIMESTAMP DEFAULT CURRENT_TIMESTAMP | 变更时间 |

#### 表7: `threshold_adjustment` — 动态修正记录

| 列名 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT PK | 主键 |
| stage_threshold_id | BIGINT FK | 被修正的阈值 ID |
| adjustment_type | VARCHAR(32) | WEATHER（天气）/ SEASONAL（季节）/ SENSOR_DRIFT（传感器漂移） |
| adjustment_factor | DECIMAL(5,3) NOT NULL | 修正系数（1.0 = 无修正，0.8 = 降低 20%，1.2 = 提高 20%） |
| effective_from | TIMESTAMP NOT NULL | 修正生效时间 |
| effective_to | TIMESTAMP | 修正失效时间（天气修正通常有明确结束时间） |
| reason | VARCHAR(512) | 修正原因，如"未来3天预报累计降雨量 >25mm" |
| is_active | BOOLEAN DEFAULT TRUE | 是否当前有效 |

#### 表8: `alert_rule` — 告警规则

| 列名 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT PK | 主键 |
| alert_name | VARCHAR(64) NOT NULL | 告警名称 |
| alert_type | VARCHAR(32) NOT NULL | DATA_LOSS / EXTREME_VALUE / TREND / DEVICE_FAULT |
| property_identifier | VARCHAR(16) | 监控的属性标识符 |
| device_name | VARCHAR(32) | 监控的设备名 |
| trigger_condition | VARCHAR(512) NOT NULL | 触发条件表达式 |
| severity | VARCHAR(16) | CRITICAL / WARNING / INFO |
| message_template | VARCHAR(512) | 告警消息模板 |
| cooldown_minutes | INT DEFAULT 30 | 告警冷却（同样告警的最小间隔） |
| is_active | BOOLEAN DEFAULT TRUE | 是否启用 |

---

## 四、预置数据（苹果种植专家知识库）

### 4.1 土壤类型基础参数

| 土壤类型 | 田间持水量(%) | 萎蔫系数(%) | 有效水量(%) | 入渗速率(mm/h) | 修正系数 |
|----------|-------------|------------|------------|---------------|---------|
| SAND（沙土） | 15 | 4 | 11 | 25 | 0.70 |
| SANDY_LOAM（沙壤土） | 22 | 8 | 14 | 15 | 0.85 |
| LOAM（壤土） | 32 | 12 | 20 | 10 | 1.00 |
| CLAY_LOAM（黏壤土） | 38 | 16 | 22 | 6 | 1.15 |
| CLAY（黏土） | 42 | 22 | 20 | 3 | 1.30 |

### 4.2 红富士苹果各物候期基础阈值（标准壤土）

| 物候期 | 土壤湿度下限 | 土壤湿度上限 | 土壤pH下限 | 土壤pH上限 | 环境温度下限 | 环境温度上限 | 光照下限(lux) |
|--------|------------|------------|-----------|-----------|------------|------------|-------------|
| 休眠期 | 50% | 70% | 5.5 | 7.5 | -15°C | 10°C | — |
| 萌芽期 | 55% | 75% | 5.8 | 7.2 | 5°C | 25°C | 15000 |
| 花期 | 60% | 75% | 6.0 | 7.0 | 8°C | 25°C | 20000 |
| 幼果期 | 60% | 80% | 6.0 | 7.0 | 12°C | 30°C | 25000 |
| 膨大期 | 65% | 80% | 6.0 | 7.0 | 18°C | 32°C | 30000 |
| 成熟期 | 55% | 70% | 6.0 | 7.0 | 15°C | 28°C | 30000 |
| 落叶期 | 50% | 70% | 5.8 | 7.2 | 0°C | 18°C | — |

### 4.3 告警规则预设

| 告警名称 | 条件 | 级别 |
|----------|------|------|
| 数据中断 | 任一传感器 > 30min 无数据 | WARNING |
| pH极端异常 | pH < 4.5 或 pH > 8.5 | CRITICAL |
| 温度极端 | 环境温度 > 40°C 持续1h | CRITICAL |
| 霜冻风险 | 花期/幼果期 + 预报温度 < 0°C | CRITICAL |
| 湿度骤降 | 土壤湿度 2h 内下降 >15% | WARNING |
| 灌溉异常 | 水泵=开 且 湿度持续下降 >30min | CRITICAL |
| 高湿病害风险 | 环境湿度 >85% 持续48h + 温度20-28°C | WARNING |

---

## 五、技术实现方案

### 5.1 Maven 依赖新增

```xml
<!-- H2 Database（嵌入式，无需安装） -->
<dependency>
    <groupId>com.h2database</groupId>
    <artifactId>h2</artifactId>
    <scope>runtime</scope>
</dependency>

<!-- Spring Data JPA -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-jpa</artifactId>
</dependency>
```

### 5.2 application.yml 新增配置

```yaml
spring:
  datasource:
    url: jdbc:h2:file:./data/threshold_db;DB_CLOSE_DELAY=-1;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update        # 自动建表/更新表结构
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    open-in-view: false
```

要点说明：
- `jdbc:h2:file:./data/threshold_db` — 持久化到文件，重启不丢数据
- `DB_CLOSE_DELAY=-1` — 最后一个连接关闭后不关闭数据库
- `MODE=MySQL` — 兼容 MySQL 语法（便于日后切换）
- `ddl-auto: update` — 启动自动建表/加列，不删数据

### 5.3 包结构规划

```
src/main/java/org/example/lightaisoilmoistirrigationbackend/
├── model/
│   ├── Result.java                          # 已有：统一响应
│   ├── entity/
│   │   ├── CropConfig.java                  # 作物配置实体
│   │   ├── SoilConfig.java                  # 土壤类型实体
│   │   ├── PhenologyStage.java              # 物候期实体
│   │   ├── StageThreshold.java              # 物候期阈值实体
│   │   ├── ThresholdRule.java               # 阈值规则实体
│   │   ├── ThresholdSnapshot.java           # 阈值快照实体
│   │   ├── ThresholdAdjustment.java         # 动态修正实体
│   │   └── AlertRule.java                   # 告警规则实体
│   └── dto/
│       ├── ThresholdQueryDTO.java           # 阈值查询请求
│       ├── ThresholdUpdateDTO.java          # 阈值更新请求
│       ├── ThresholdResultDTO.java          # 阈值计算结果
│       └── AlertEventDTO.java               # 告警事件
├── repository/
│   ├── CropConfigRepository.java
│   ├── SoilConfigRepository.java
│   ├── PhenologyStageRepository.java
│   ├── StageThresholdRepository.java
│   ├── ThresholdRuleRepository.java
│   ├── ThresholdSnapshotRepository.java
│   ├── ThresholdAdjustmentRepository.java
│   └── AlertRuleRepository.java
├── service/
│   ├── DeviceDataManager.java               # 已有：数据管理
│   ├── OneNetApiClient.java                 # 已有：API 客户端
│   ├── ThresholdService.java                # 新增：阈值 CRUD + 查询
│   ├── ThresholdEvaluator.java              # 新增：阈值计算引擎
│   ├── PhenologyService.java                # 新增：物候期判断
│   ├── AlertService.java                    # 新增：告警判断
│   └── DataInitializer.java                 # 新增：预置数据初始化
└── controller/
    ├── DeviceController.java                # 已有：设备数据接口
    └── ThresholdController.java             # 新增：阈值管理接口
```

### 5.4 核心服务设计

#### ThresholdService — 阈值 CRUD

```
接口：
  GET    /api/thresholds/stages/{stageId}        查询某物候期的所有阈值
  GET    /api/thresholds/current?cropId=&soilId= 查询当前生效的阈值（自动判断物候期）
  PUT    /api/thresholds/{id}                    更新单个阈值（自动记录快照）
  POST   /api/thresholds/batch                  批量更新阈值
  GET    /api/thresholds/history/{thresholdId}   查看阈值变更历史
  GET    /api/thresholds/adjustments             查询当前所有生效的动态修正
  POST   /api/thresholds/adjustments             创建动态修正
  DELETE /api/thresholds/adjustments/{id}        取消动态修正
```

#### ThresholdEvaluator — 阈值计算引擎

```
核心方法：
  evaluateEffectiveThreshold(deviceName, propertyIdentifier, soilType)
    → 返回该属性在当前条件下的最终有效阈值

计算流程：
  1. 获取当前作物配置
  2. 调用 PhenologyService 判断当前物候期
  3. 从 stage_threshold 表读取该物候期的基础阈值
  4. 根据 soil_config 的修正系数调整
  5. 叠加 threshold_adjustment 表中所有生效的动态修正
  6. 返回最终阈值 + 各层修正明细（用于前端展示和决策解释）
```

#### PhenologyService — 物候期判断

```
核心方法：
  determineCurrentStage(cropConfigId)
    → 返回当前所处的物候期

判断逻辑（优先级从高到低）：
  1. 如果 OneNet 有积温数据 → 用 GDD（生长度日）对照 phenology_stage 的 gdd_threshold 判断
  2. 如果没有积温数据 → 用当前日期对照 typical_start_month / typical_end_month 粗略判断
  3. 如果果农手动指定了当前阶段 → 以手动指定为准
```

#### AlertService — 告警判断

```
核心方法：
  evaluateAlerts(deviceDataMap)
    → 遍历所有启用的 alert_rule，返回触发的告警列表

执行频率：
  - 与现有 2 秒数据刷新同频
  - 每个告警规则有独立的冷却计时器，避免刷屏
```

### 5.5 与现有系统的集成点

```
现有数据流：
  OneNet → DeviceDataManager(2s轮询) → Controller → 前端SSE

集成后的数据流：
  OneNet → DeviceDataManager(2s轮询) → 更新缓存
                                      ↓
                                ThresholdEvaluator（计算有效阈值）
                                      ↓
                                AlertService（判断是否触发告警）
                                      ↓
                                合并后数据 → Controller → 前端SSE
                                      ↓
                                阈值偏离 → RuleEngine → LLM 决策
```

改动点：
1. **DeviceDataManager 新增回调钩子**：每次刷新数据后，通知 ThresholdEvaluator 和 AlertService
2. **Controller `/api/latest` 增强**：返回数据中附带 `effective_thresholds` 和 `active_alerts`
3. **前端仪表盘**："设备数据决策"标签页接入真实阈值数据，显示当前值 vs 阈值的对比

### 5.6 数据初始化策略

使用 Spring Boot `CommandLineRunner` 实现 `DataInitializer`：

```java
@Component
public class DataInitializer implements CommandLineRunner {
    // 启动时检查 crop_config 表是否为空
    // 如果为空 → 插入预置的苹果种植阈值数据（4.2 节内容）
    // 如果不为空 → 跳过（保留用户修改）
}
```

这样首次启动自动填充专家知识库，后续用户修改不受影响。

---

## 六、API 接口设计

### 6.1 阈值查询接口

```
GET /api/thresholds/current
  参数：device_name（必填）, soil_type（选填，默认 LOAM）
  返回：
  {
    "code": 0,
    "data": {
      "crop": {"name": "苹果", "variety": "红富士"},
      "current_stage": {"name": "膨大期", "stage_order": 5},
      "thresholds": {
        "soil_moisture_lower": {
          "base_value": 65.0,
          "soil_correction": 0.85,
          "weather_adjustment": 0.9,
          "effective_value": 49.7,
          "unit": "%",
          "current_value": 42.0,
          "status": "BELOW"      // NORMAL / BELOW / ABOVE / CRITICAL
        },
        "pH_lower": { ... },
        "pH_upper": { ... }
      },
      "alerts": [
        {"type": "WARNING", "message": "土壤湿度低于膨大期下限，建议灌溉"}
      ]
    }
  }
```

### 6.2 阈值管理接口

```
GET    /api/thresholds/stages             查询所有物候期列表
GET    /api/thresholds/stages/{id}        查询某物候期的所有阈值
PUT    /api/thresholds/{id}               更新单个阈值
POST   /api/thresholds/adjustments        创建临时修正
GET    /api/thresholds/history/{id}       查看阈值变更历史
GET    /api/thresholds/alerts             查询当前活跃告警
GET    /api/thresholds/alerts/rules       查询告警规则列表
PUT    /api/thresholds/alerts/rules/{id}  更新告警规则
```

### 6.3 作物/土壤配置接口

```
GET    /api/config/crops                  查询作物配置
PUT    /api/config/crops/{id}             更新作物配置
GET    /api/config/soils                  查询土壤类型列表
PUT    /api/config/soils/{id}             更新土壤参数
POST   /api/config/phenology/override     手动指定当前物候期
DELETE /api/config/phenology/override     取消手动指定，恢复自动判断
```

---

## 七、前端集成方案

### 7.1 阈值管理页面

在现有的"设备数据决策"标签页中新增子标签或内容：

- **阈值配置面板**：表格展示各物候期的阈值，支持在线编辑
- **当前阈值状态**：实时显示有效阈值 vs 传感器当前值，用颜色标识状态
- **修正管理**：显示当前生效的动态修正，支持手动添加/取消
- **历史变更记录**：阈值修改的时间线

### 7.2 数据展示增强

在每个传感器数值旁增加阈值参考线和状态标签：
```
土壤湿度: [====42%====]  ⚠️ 低于下限 49.7%
           ^          ^           ^
          阈值下限    当前值      阈值上限
```

---

## 八、后续演进方向

| 阶段 | 内容 | 依赖 |
|------|------|------|
| **一期（当前）** | H2DB + JPA 建表、预置苹果阈值数据、基础 CRUD 接口、物候期自动判断、阈值查询 API | 无新增依赖 |
| **二期** | 动态修正（天气联动）、告警规则引擎、前端阈值管理页面 | 需接入天气 API |
| **三期** | 阈值自动校准（基于历史数据回归分析）、LLM 决策集成使用阈值偏离作为输入、多作物支持 | 需积累足够历史数据 |

---

## 九、关键设计决策记录

| 决策 | 选择 | 理由 |
|------|------|------|
| 数据库 | H2DB 文件模式 | 嵌入式免安装，重启保留数据，支持 MySQL 兼容模式便于日后迁移 |
| ORM | Spring Data JPA | 自动建表（ddl-auto:update），方法命名查询减少模板代码 |
| 阈值模型 | 基准值 + 修正链 | 单一固定值无法适应农业场景的季节/土壤/天气变化 |
| 阈值历史 | snapshot 表 | 完整审计追踪，支持回溯和分析阈值调整效果 |
| 表结构分离 | 物候期阈值 与 动态修正 分开 | 基准是专家知识（稳定），修正是实时环境响应（动态），职责清晰 |
| 预置数据 | CommandLineRunner | 首次启动自动填充，后续不覆盖用户修改 |